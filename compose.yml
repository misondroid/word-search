services:
  app:
    build:
      context: .
      dockerfile: services/app/Dockerfile
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=development
    volumes:
      - ./code/frontend:/app
      - /app/node_modules
    working_dir: /app
    command: npm run dev